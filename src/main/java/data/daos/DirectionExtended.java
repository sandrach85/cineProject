package data.daos;

import data.entities.Direction;

public interface DirectionExtended {
	public boolean addDirection(Direction direction);
	public boolean removeDirection(Direction direction);
}
